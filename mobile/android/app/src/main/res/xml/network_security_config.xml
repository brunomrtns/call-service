<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Para desenvolvimento, permitir certificados auto-assinados -->
    <debug-overrides>
        <trust-anchors>
            <!-- Confiar em certificados do sistema -->
            <certificates src="system"/>
            <!-- Confiar em certificados personalizados/auto-assinados -->
            <certificates src="user"/>
        </trust-anchors>
    </debug-overrides>
    
    <!-- Configuração para rede local (permite descoberta automática) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <!-- Permite qualquer IP da rede local 192.168.x.x -->
        <domain includeSubdomains="true">192.168.0.0/16</domain>
        <!-- Permite qualquer IP da rede local 10.x.x.x -->
        <domain includeSubdomains="true">10.0.0.0/8</domain>
        <!-- Permite qualquer IP da rede local 172.16.x.x - 172.31.x.x -->
        <domain includeSubdomains="true">172.16.0.0/12</domain>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </domain-config>
</network-security-config>
